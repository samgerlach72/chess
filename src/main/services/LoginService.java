package services;
import responses.LoginResponse;
import requests.LoginRequest;

/**
 * service to login
 */
public class LoginService {
    /**
     * Logs a user into db
     *
     * @param request is request of User to Login
     * @return of type LoginResponse is formatted response with new authToken
     */
    public LoginResponse login(LoginRequest request) {
        return null;
    }
}
